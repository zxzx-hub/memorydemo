日志显示商店在 CN/zh-CN 市场请求 storeedgefd.dsx.mp.microsoft.com 时网络层报 0x80072EFD，这就是 Store 页面 0x80131500 的底层原因。我再收窄查和 9PLM9XGG6VKS / OpenAI.Codex 直接相关的事件。




已运行 4 条命令已搜索网页已运行 $since=(Get-Date).AddHours(-2); Get-WinEvent -FilterHashtable @{LogName='Microsoft-Windows-Store/Operational'; StartTime=$since} -ErrorAction SilentlyContinue | Where-Object { $_.Message -match '9PLM9XGG6VKS|OpenAI|Codex|ChatGPT|cd8fa55e' } | Select-Object TimeCreated,Id,LevelDisplayName,Message | Format-List，已持续 21s



已运行 $since=(Get-Date).AddHours(-2); Get-WinEvent -FilterHashtable @{LogName='Application'; StartTime=$since} -ErrorAction SilentlyContinue | Where-Object { $_.Message -match '9PLM9XGG6VKS|OpenAI|Codex|ChatGPT|cd8fa55e' } | Select-Object TimeCreated,ProviderName,Id,LevelDisplayName,Message | Format-List，已持续 19s



已运行 $since=(Get-Date).AddHours(-2); Get-WinEvent -FilterHashtable @{LogName='Microsoft-Windows-AppXDeploymentServer/Operational'; StartTime=$since} -ErrorAction SilentlyContinue | Where-Object { $_.Message -match '9PLM9XGG6VKS|OpenAI|Codex|ChatGPT|cd8fa55e' } | Select-Object TimeCreated,Id,LevelDisplayName,Message | Format-List，已持续 18s



已运行 $pkg=Get-AppxPackage -Name 'OpenAI.Codex'; $manifest=Get-AppxPackageManifest -Package $pkg.PackageFullName; [pscustomobject]@{Name=$pkg.Name; PackageFullName=$pkg.PackageFullName; DisplayName=$manifest.Package.Properties.DisplayName; PublisherDisplayName=$manifest.Package.Properties.PublisherDisplayName; Version=$pkg.Version; InstalledLocation=$pkg.InstallLocation} | Format-List，已持续 10s



ChatGPT Windows installer OpenAI | download.openai.com












官方安装通道被本机旧版商店卡住了；现在可行的稳妥绕法是只用第三方页面解析“微软 CDN 的直链”，下载后用 Windows 签名和包身份校验，真正安装的仍必须是 Microsoft Marketplace 签名的 OpenAI.Codex MSIX。我要先抓取候选链接，不会安装不匹配的包。